amends "pkl:Project"

// Package configuration for local development.
// Update baseUri and packageZipUrl when publishing to a registry.
package {
  name = "proxmox"
  baseUri = "package://localhost/plugins/proxmox/schema/pkl/proxmox"
  version = "0.1.0"
  packageZipUrl = "https://localhost/plugins/proxmox/schema/pkl/proxmox@\(version).zip"
}

dependencies {
  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.80.1"
  }
}
